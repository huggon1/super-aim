# Unit Tests

This folder is reserved for pure logic tests such as `ScoreTracker` and `TrainingSession`.

The current environment does not expose a `godot` command, so automated Godot tests are not wired yet.
